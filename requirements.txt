# Core RAG stack — all pure-pip, no system binaries (Windows/CPU friendly)
python-dotenv==1.2.2

# LangChain (v1 line)
langchain==1.3.11
langchain-core==1.4.8
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-groq==1.1.3
langchain-chroma==1.1.0
langchain-huggingface==1.2.2

# Vector store
chromadb==1.5.9

# Embeddings + cross-encoder reranker (CPU)
sentence-transformers==5.6.0
torch==2.12.1
transformers==5.12.1

# Lexical retrieval
rank-bm25==0.2.2

# PDF parsing + table extraction (no Poppler/Tesseract needed)
pdfplumber==0.11.10

# OCR fallback for scanned pages / images of text (pure-pip ONNX, bundled
# models, CPU — page rendering via pdfplumber's pypdfium2, still no binaries)
rapidocr-onnxruntime==1.4.4

# Web UI
gradio==6.19.0
